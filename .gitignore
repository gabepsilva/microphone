.venv-moonshine/
__pycache__/
*.py[cod]
.coverage
.pytest_cache/
coverage.xml
coverage.json
reports/
mutants/
.mutmut-cache

# Build artifacts from `uv build`
dist/
*.egg-info/

# Electron / npm (installed locally via `npm install electron --no-save`)
node_modules/

# Generated audio and transcription output
virtual-mic-*.mp3
virtual-mic-*.wav
virtual-out-*.mp3
virtual-out-*.wav
transcription/
tagalong.yaml

# Claude Code scratch worktrees — a second copy of the package, not source.
# Skills are source: they link to the canonical copies under .agents/skills/.
.claude/*
!.claude/skills/
